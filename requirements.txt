# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.0

# Config / env
pydantic-settings==2.3.0
python-dotenv==1.0.0

# HTTP
httpx>=0.28.1,<1.0.0  # rango abierto, no version exacta -- anthropic exige
                      # >=0.25.0 y google-genai exige >=0.28.1, un pin
                      # exacto como el de antes (0.27.0) choca con ambos
requests==2.32.3

# Data
pandas==2.2.3
numpy==1.26.4
yfinance==0.2.66
anthropic==0.117.0  # agente Bull (tesis) — ver conversacion 17/07/2026
google-genai==2.12.1  # agente Bull, modo de prueba gratuito (Gemini)
xlrd==2.0.2  # lectura de .xls antiguo (dataset CAPE de Shiller, Yale)

# Timezone
pytz==2024.1
Pillow==12.3.0  # redimensionar la foto de Gael para Telegram
