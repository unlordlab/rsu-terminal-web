# Los scripts de shell se ejecutan en el VPS (Linux). Con core.autocrlf en
# Windows, un checkout los dejaria con fin de linea CRLF y bash fallaria con
# "command not found". Aqui se fija LF en todas las copias.
*.sh text eol=lf
